/**
 * HistoryPage — shows past predictions with analytics charts
 * TODO: fetch from /history endpoint and render with Recharts
 */
export default function HistoryPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold mb-4">Prediction History</h1>
      {/* TODO: fetch history, render table + pie chart */}
      <p className="text-gray-500">No predictions yet.</p>
    </div>
  );
}
